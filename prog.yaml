session_name: prog
windows:
- window_name: prog
  layout: tiled
  shell_command_before:
    - cd ~/Program_File        # run as a first command in all panes
  panes:
    - shell_command:           # pane no. 1
        - vim                  # run multiple commands in this pane
